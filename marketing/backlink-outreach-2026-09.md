# Backlink outreach — September 2026

## Response log

| Date (UTC) | Target | Result | Status |
|---|---|---|---|
| 2026-09-11 | memeburn.com — carl.davis@memeburn.com | Offered paid press releases, partnered articles, and banner placements ($600–$1,200 on MemeBurn; $400–$900 on NFT Plazas/NFT Evening). Replied that AIToolsEssentials is not buying sponsored coverage and was seeking an editorial resource mention only. | Closed — paid only |
| 2026-09-11 | toolscout.ai — contact@toolscout.ai | Asked what AIToolsEssentials would pay for a sponsored feature/resource placement. Replied that no budget is allocated to paid placement and the request was for an editorial mention only. Their identical pitch arrived a second time (06:50 UTC) — same ask, no reply needed. | Closed — paid only |
| 2026-09-09 | techsy.io — hello@techsy.io | Rates offered: $170 into an existing roundup, $86 standalone review, $238 both (dofollow, no sponsored tag, permanent, published across 22 language versions). Declined paid placement; they then proposed a reciprocal **link exchange** and asked for our DR and monthly traffic. Declined the swap too — early site, no DR/traffic disclosure, and we do not trade undisclosed dofollow links. | Closed — paid/link-swap only |
| 2026-09-10 | zplatform.ai — alston@zplatform.ai | Guest-post pitch answered with $95 per article "or link insertion", one permanent dofollow link, no sponsored tag, 700-word minimum. Declined paid placement and undisclosed dofollow links. | Closed — paid only |
| 2026-09-09 | uneed.best — contact@uneed.best | Keep/cut resource note answered only by their support auto-ack ("support team available 9:00–18:00 UTC+1 weekdays"), no human reply. Site also still requires an account before a submission can be saved, so the directory side stays George-blocked. | Open — auto-ack only |
| 2026-09-11 | stackaible.com — info@stackaible.com | **Hard bounce (550 no such user)** for the Sept 9 keep/cut resource note. The site publishes no email address on `/`, `/about`, `/privacy`, `/terms`, `/system`, or `/newsletter` — operator David Bowers is reachable only via X/YouTube (`@aiorchestration`). No retry: no valid published address. | Closed — unreachable |
| 2026-09-11 | videoupscaler.video — support@videoupscaler.video | **Hard bounce (550 5.1.1 address does not exist)** for the Sept 7 verification request. The address is still printed in the site's own privacy and terms pages, so the vendor's published contact is broken. Domain has MX (Cloudflare Email Routing), so a different address may work. | Blocked — vendor must publish a working address |
| 2026-09-11 | aigearbase.com — contact@aigearbase.com | **Delayed bounce, then confirmed dead end.** Domain has **no MX records at all** (Cloudflare NS `todd`/`jean`; apex A records 172.67.164.102 / 104.21.50.168; port 25 times out). The address is printed on `/contact` only as a Cloudflare-obfuscated `data-cfemail` token that decodes to that same dead address, and the contact form is a JS stub (`alert()` + `reset()`, no fetch, no `/api/contact`, `/contact.php` → 404). No reachable channel exists. Do not retry. | Closed — unreachable |
| 2026-09-11 | aitoolswise.com — contact@aitoolswise.com | **Delayed bounce, then confirmed dead end.** Domain has no MX records at all; apex sends a 308 to `https://aitoolswise.com/`, and HTTPS then fails with a TLS `tlsv1 alert internal error` behind Caddy at 65.108.89.221. The site is only reachable via Google's cache/index. Domain registered 2026-02-08 (Cloudflare NS). Do not retry. | Closed — unreachable |
| 2026-09-11 | llmversus.com — hello@llmversus.com | **Silent dead end, no bounce ever received.** The Sept 9 keep/cut note was filed to `[Gmail]/Trash` (Gmail does this for some sends) and the domain publishes **no MX records**, so the address cannot receive mail. The site itself is live (HTTP 200 on `/`, `/contact`, `/about`, `/privacy`, `/terms`) behind Cloudflare at 216.198.79.1 — a working website with an undeliverable mailbox, the same trap as aigearbase.com. | Closed — unreachable |

## Delivery-failure handling notes

- **Corrected sweep v2 (2026-09-11, authoritative).** A `[Gmail]/Sent Mail`-only sweep undercounts, because Gmail files some outbound messages in `[Gmail]/Trash`. Scanning Sent + Trash and deduplicating by (date, subject, recipient) gives the real September picture:
  - **September 2026 outbound: 78 sends across 68 distinct recipient domains.**
  - **Sept 9 batch alone: 61 sends across 58 distinct domains** (Sent Mail contributed 59/56; Trash held 2 more — `contact@aigearbase.com` and `hello@llmversus.com`).
  - **Three September domains have no MX at all: `aitoolswise.com`, `aigearbase.com`, `llmversus.com`.** All three are dead ends.
  - This supersedes both earlier figures ("exactly one no-MX domain" and then "52 sends / two no-MX domains"). Both were wrong for the same root cause: the sweep source was wrong, not just incomplete. A curated target list misses recipients added outside it; Sent-Mail-only misses sends Gmail filed to Trash.
- Lesson: MX-check the **exact domain from the `To:` header of every send**, drawn from **every folder that can hold outbound mail** — not a curated target list and not Sent Mail alone.
- Two hard bounces on Sept 11 were for addresses the vendors themselves publish (`support@videoupscaler.video`) or that the outreach list assumed (`info@stackaible.com`). Neither is worth a retry without a working address from the vendor.
- Bounce subjects arrive as `Delivery Status Notification (Delay)` first and `(Failure)` later; a Delay is not actionable on its own — check the referenced `To:` header inside the attached `message/rfc822` block, not the `mailer-daemon` envelope.
- **A Delay is not evidence of a dead end by itself, and a live website is not evidence of a working mailbox.** `aigearbase.com` and `llmversus.com` both return HTTP 200 with polished content and publish a contact address; both domains have no MX at all. `aigearbase.com` also has a non-functional contact form (`alert()` + `reset()`, no fetch, `/api/contact` and `/contact.php` → 404). Always resolve MX before treating a published contact as reachable.
- **No MX means no bounce will ever arrive.** `llmversus.com` produced no DSN at all, so it would have stayed invisible to bounce-driven detection indefinitely. Absence of a bounce is not evidence of delivery.
